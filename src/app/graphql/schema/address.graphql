scalar PostalCode

scalar Municipality

scalar Country

scalar Phone

scalar Email

type Address {
  streetAddress: String!
  postalCode: PostalCode!
  municipality: Municipality!
  country: Country!
}

input AddressInput {
  streetAddress: String!
  postalCode: PostalCode!
  municipality: Municipality!
  country: Country!
}
