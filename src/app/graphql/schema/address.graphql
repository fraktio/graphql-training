scalar PostalCode

scalar Municipality

scalar CountryCode

scalar Phone

scalar Email

type Address {
  streetAddress: String!
  postalCode: PostalCode!
  municipality: Municipality!
  country: CountryCode!
}

input AddressInput {
  streetAddress: String!
  postalCode: PostalCode!
  municipality: Municipality!
  country: CountryCode!
}
